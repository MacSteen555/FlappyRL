# Unit Tests

Unit tests for FlappyRL using Catch2.

## Running Tests

After building with `BUILD_TESTING=ON`:

```bash
cd build
ctest --output-on-failure
```

Or run the test executable directly:
```bash
./bin/FlappyRL_tests
```

